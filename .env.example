DATABASE_URL=mysql://root:password@db:3306/mini_crm
# Tip: to limit per-process DB connections (helps on small hosts),
# you can append `?connection_limit=5` to the URL:
# DATABASE_URL=mysql://root:password@db:3306/mini_crm
SESSION_SECRET=replace-with-strong-random-string
TZ=Asia/Tashkent

# Optional for NextAuth (if switching to it)
NEXTAUTH_SECRET=replace-with-strong-random-string
NEXTAUTH_URL=http://localhost:3000

# App ports
ADMIN_PORT=3000
POS_PORT=4000
